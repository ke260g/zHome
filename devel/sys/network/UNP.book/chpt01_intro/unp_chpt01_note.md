### Intro
+ uucp, unix to unix copy
+ protocol independence

+ daytime_client: connect()
+ daytime_server: bind(), listen(), accept()
+ snprintf()

+ iterative server, concurrent server

+ OSI model(7): [app, presentation, session], transport, network, datalink, physical
+ Internet Protocol suite: [app], tp(TCP/UDP), ip(ipv4/ipv6), device_driver, hardware

+ 1.8 BSD network history£¬not about the Linux, MacOS/X, Solaris, etc.


+ uucp, unix to unix copy
+ protocol independence

+ daytime_client: connect()
+ daytime_server: bind(), listen(), accept()
+ snprintf()

+ iterative server, concurrent server

+ OSI model(7): [app, presentation, session], transport, network, datalink, physical
+ Internet Protocol suite: [app], tp(TCP/UDP), ip(ipv4/ipv6), device_driver, hardware

+ 1.8 BSD network history£¬not about the Linux, MacOS/X, Solaris, etc.
